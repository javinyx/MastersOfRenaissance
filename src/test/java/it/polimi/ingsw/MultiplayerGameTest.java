package it.polimi.ingsw;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class MultiplayerGameTest{

    @Test
    public void testCreatePlayer() {

        /*Game game = new MultiplayerGame();

        game.createPlayer("javis");
        game.createPlayer("coco");
        game.createPlayer("ale");
        game.createPlayer("RICK.");

        for(int i=1; i<=4; i++){
            assertEquals(i, game.getPlayers().get(i-1).getTurnID());
        }

        assertEquals("javis", game.getPlayers().get(0).getNickname());
        assertEquals("coco", game.getPlayers().get(1).getNickname());
        assertEquals("ale", game.getPlayers().get(2).getNickname());
        assertEquals("rick", game.getPlayers().get(3).getNickname());
    */
    }
}